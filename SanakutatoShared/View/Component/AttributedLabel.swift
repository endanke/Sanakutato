//
//  AttributedLabel.swift
//  SanakutatoShared
//
//  Created by Daniel Eke on 2020. 08. 29..
//

import SwiftUI
import Combine

/*
struct AttributedLabel: Representable {

    //@Binding var content: String
    static var cancellables = Set<AnyCancellable>()

    #if os(macOS)

    func makeNSView(context: RepresentableContext<Self>) -> NSTextField {
        let view = NSTextField(frame: .zero)
        view.isEditable = false
        view.isSelectable = false
        view.lineBreakMode = .byWordWrapping
        view.maximumNumberOfLines = 0
        view.translatesAutoresizingMaskIntoConstraints = false
        view.setContentCompressionResistancePriority(.defaultLow, for: .horizontal)
        view.setContentCompressionResistancePriority(.required, for: .vertical)
        return view
    }

    func updateNSView(_ view: NSTextField, context: RepresentableContext<Self>) {}

    #else

    #endif

}
*/
